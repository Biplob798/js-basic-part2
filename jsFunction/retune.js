function tenTimes(number) {
    const result = number * 10;
    return result;
    console.log(object);//no work
}
+
    function cutHalf(number) {
        const half = number / 2;
        return half;
    }

tenTimes(12)
const output = tenTimes(12);
console.log(output);

const bigNumber = tenTimes(35)


function add(p1, p2) {
    const total = p1 + p2;
    return total;
}

add()
const bill = add(12, 25)


function add2(price1, price2) {
    return price1 + price2
}
const bill2 = add2(12, 55)

function doMath(n1, n2) {
    const sum = n1 + n2;
    const diff = n1 - n2;
    const mul = sum * diff;
    const result = mul / 2;
    return result;
}
const result=doMath(12,6)

