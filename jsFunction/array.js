// sum of a array in all numbers

function sumOfNumbers(numbers) {
    let sum = 0;
    for (const number of numbers) {
        console.log(number);
        sum = sum + number;
    }
}
return sum;














































































































































































































const numbs = [12, 21, 52, 23];
const sum = sumOfNumbers(numbs);
console.log('Sum of numbers is', sum);